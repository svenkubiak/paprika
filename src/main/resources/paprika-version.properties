# Filled in by Maven resource filtering at build time, see the filtered <resource> entry in
# pom.xml. Deliberately not named version.properties: mangoo reads a resource under that exact
# name from the classpath root for its own version, and both end up in the shaded uber jar.
version=${project.version}
